import { Link } from "@tanstack/react-router";
import { Menu, Phone, X, MessageCircle, Mail, MapPin, Clock } from "lucide-react";
import { useState } from "react";
import { BrandMark } from "./brand-mark";
import { Button } from "./button";
import { OpenStatus } from "./open-status";
import { whatsappFor } from "@/data/menu";
const links = [{to:"/" as const,label:"Home"},{to:"/menu" as const,label:"Menu"},{to:"/about" as const,label:"About"},{to:"/contact" as const,label:"Contact"}];
export function Header(){ const [open,setOpen]=useState(false); return <header className="site-header"><div className="site-container flex h-20 items-center justify-between"><Link to="/" aria-label="Sithara Grand home"><BrandMark /></Link><nav className="hidden items-center gap-8 md:flex" aria-label="Main navigation">{links.map(l=><Link key={l.to} to={l.to} activeOptions={{exact:l.to==="/"}} className="nav-link" activeProps={{className:"nav-link nav-link-active"}}>{l.label}</Link>)}</nav><div className="hidden md:block"><Button asChild size="sm"><a href="tel:+919121577737"><Phone className="size-4"/>Call now</a></Button></div><Button variant="ghost" size="icon" className="md:hidden" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Toggle menu">{open?<X/>:<Menu/>}</Button></div>{open&&<nav className="mobile-menu" aria-label="Mobile navigation">{links.map(l=><Link key={l.to} to={l.to} onClick={()=>setOpen(false)} className="mobile-nav-link">{l.label}</Link>)}<Button asChild><a href="tel:+919121577737"><Phone className="size-4"/>Call now</a></Button></nav>}</header> }
export function Footer(){return <footer className="border-t border-border bg-brand-deep"><div className="site-container grid gap-12 py-16 md:grid-cols-[1.2fr_.7fr_1fr_1fr]"><div><BrandMark/><p className="mt-5 max-w-xs text-sm leading-7 text-muted-foreground">Premium Multi Cuisine Family Restaurant in Piduguralla.</p></div><div><h2 className="footer-title">Navigate</h2>{links.map(l=><Link key={l.to} to={l.to} className="footer-link">{l.label}</Link>)}</div><div><h2 className="footer-title">Contact</h2><a className="footer-link" href="tel:+919121577737"><Phone/>+91 91215 77737</a><a className="footer-link break-all" href="mailto:sitaragrandpdrl@gmail.com"><Mail/>sitaragrandpdrl@gmail.com</a><p className="footer-link"><MapPin/>Near By Pass Junction, Piduguralla - 522413</p></div><div><h2 className="footer-title">Hours</h2><p className="footer-link"><Clock/>Monday–Sunday<br/>12:00 PM – 11:30 PM</p><OpenStatus/></div></div><div className="border-t border-border"><div className="site-container py-6 text-xs text-muted-foreground">© 2026 Sithara Grand Family Restaurant. All rights reserved.</div></div></footer>}
export function FloatingWhatsApp(){return <a className="floating-whatsapp" href={whatsappFor()} target="_blank" rel="noreferrer" aria-label="Enquire on WhatsApp"><MessageCircle className="size-6"/></a>}
export function SiteShell({children}:{children:React.ReactNode}){return <><Header/><main>{children}</main><Footer/><FloatingWhatsApp/></>}
