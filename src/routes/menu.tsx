import { createFileRoute } from "@tanstack/react-router";
import { Search } from "lucide-react";
import { useMemo, useState } from "react";
import { menuCategories, menuItems, type Dietary } from "@/data/menu";
import { MiniMenuCard } from "@/components/mini-menu-card";
export const Route=createFileRoute("/menu")({validateSearch:(s:Record<string,unknown>)=>({category:typeof s["category"]==="string"?s["category"]:undefined}),head:()=>({meta:[{title:"Complete Menu | Sithara Grand Piduguralla"},{name:"description",content:"Search the complete Sithara Grand menu with exact prices for biryanis, starters, curries, breads, desserts and drinks."},{property:"og:title",content:"Complete Menu | Sithara Grand"},{property:"og:description",content:"Explore every dish and exact price from the official Sithara Grand menu."},{property:"og:type",content:"website"},{property:"og:url",content:"/menu"},{name:"twitter:card",content:"summary_large_image"}],links:[{rel:"canonical",href:"/menu"}]}),component:MenuPage});
function MenuPage(){const searchParams=Route.useSearch();const [query,setQuery]=useState("");const [category,setCategory]=useState(searchParams["category"]&&menuCategories.includes(searchParams["category"])?searchParams["category"]:"ALL");const [diet,setDiet]=useState<"all"|Dietary>("all");const filtered=useMemo(()=>menuItems.filter(i=>(category==="ALL"||i.category===category)&&(diet==="all"||i.dietary===diet)&&i.name.toLowerCase().includes(query.toLowerCase())),[query,category,diet]);return <><section className="page-hero"><div className="site-container"><span className="eyebrow">Official menu</span><h1>EVERY FLAVOUR.<br/><em>EXACTLY AS SERVED.</em></h1><p>{menuItems.length} items across {menuCategories.length} categories, with prices from the official Sithara Grand menu.</p></div></section><section className="menu-section"><div className="site-container"><div className="menu-toolbar"><label className="search-box"><Search/><span className="sr-only">Search menu</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search dishes…"/></label><div className="diet-filters" aria-label="Dietary filter">{(["all","veg","nonVeg","egg"] as const).map(d=><button key={d} className={diet===d?"filter-active":""} onClick={()=>setDiet(d)}>{d==="all"?"All":d==="nonVeg"?"Non-veg":d==="egg"?"Egg":"Veg"}</button>)}</div></div><div className="category-tabs" aria-label="Menu categories"><button className={category==="ALL"?"category-active":""} onClick={()=>setCategory("ALL")}>ALL</button>{menuCategories.map(c=><button key={c} className={category===c?"category-active":""} onClick={()=>setCategory(c)}>{c}</button>)}</div><p className="result-count">Showing {filtered.length} item{filtered.length===1?"":"s"}</p>{filtered.length?<div className="menu-grid">{filtered.map(i=><MiniMenuCard key={i.id} item={i}/>)}</div>:<div className="empty-menu"><h2>No dishes found</h2><p>Try another search or category.</p></div>}</div></section></>}
